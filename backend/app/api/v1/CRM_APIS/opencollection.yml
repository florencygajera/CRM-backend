opencollection: 1.0.0

info:
  name: CRM_APIS
config:
  proxy:
    inherit: true
    # Proxy configuration removed - empty values detected
    # To enable proxy, provide actual values:
    # config:
    #   protocol: http
    #   hostname: "proxy.example.com"
    #   port: "8080"
    #   auth:
    #     username: "user"
    #     password: "password"
    #   bypassProxy: false

request:
  auth:
    type: bearer
  bundled: false
  extensions:
    bruno:
      ignore:
        - node_modules
        - .git
